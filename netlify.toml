[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Decap CMS admin route
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

# Pas de redirect supplémentaire ici — _redirects (à la racine) reste en place
